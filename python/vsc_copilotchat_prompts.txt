how do i read a geopackage file into python?
For my geodataframe, I want to create a heatmap for every year (YEARAM) from 1750 to 1850.
This americas_bbox = (-100, -30, 0, 40) but for the crs 3857
now for the df, for every year in the YEAR column, create a own dataset
export them as csv files
ok, now create mutliple shapefiles based on this data for every year join with my data from the above exported files
only keep the columns for ADMIN, ISO_A3 from the shapefile and Entity, Code, Year, colonizer_grouped from the csv file
now for the df, for every year in the YEAR column, create a own dataset
now take the data from the gdf and for every year between 1700 and 1900, take the journeys and filter them based on only journeys to british colonies. Those can be found in the following yearly shapefiles: C:\\..
export them to C:\\.. as one file
no do the whole thing per year, first load the shapefile, then filter it for british colonies, then spatially filter within those areas (per year), then do the next year, append the results to a geodataframe, in the end, export it 
make sure, that all files are in WebMercator!!
and now like this, but all data combined, not seperated per country:\r\nimport matplotlib.pyplot as plt\r\nimport seaborn as sns\r\nimport matplotlib.ticker as mticker\r\n\r\nslave_voyages = british_journeys_gdf\r\n\r\n# Drop missing values in relevant columns and ensure YEARAM is integer\r\nslave_voyages = slave_voyages.dropna(subset=['YEARAM', 'SLAARRIV', 'NATINIMP'])\r\nslave_voyages['YEARAM'] = slave_voyages['YEARAM'].astype(int)\r\n\r\n# Group by country and year\r\nsv_grouped = slave_voyages.groupby(['NATINIMP', 'YEARAM'], as_index=False)['SLAARRIV'].sum()\r\n\r\n# Filter selected countries\r\ncountries = [\"Great Britain\ \"France\ \"Netherlands\ \"USA\ \"Spain / Uruguay\ \"Portugal / Brazil\"]\r\n#countries = [\"France\ \"Netherlands\ \"USA\ \"Spain / Uruguay\ \"Portugal / Brazil\"]\r\nsv_grouped_filtered = sv_grouped[sv_grouped['NATINIMP'].isin(countries)]\r\n\r\n# Apply 15-year smoothing\r\nsv_grouped_filtered['SLAARRIV_SMOOTH'] = (\r\n sv_grouped_filtered\r\n .groupby('NATINIMP')['SLAARRIV']\r\n .transform(lambda x: x.rolling(window=15, center=True, min_periods=1).mean())\r\n)\r\n\r\n# Set up the plot\r\nplt.figure(figsize=(14, 10))\r\nsns.set_theme(style=\"darkgrid\")\r\n\r\n# Plot smoothed data (solid lines)\r\nsns.lineplot(\r\n data=sv_grouped_filtered,\r\n x='YEARAM',\r\n y='SLAARRIV_SMOOTH',\r\n hue='NATINIMP',\r\n palette=clist,\r\n linewidth=2\r\n)\r\n\r\n# Add vertical lines for 1807 and 1833\r\nplt.axvline(x=1807, color='black', linestyle='--', linewidth=1)\r\nplt.text(1807 - 4, plt.ylim()[1] * 0.99, \"1807 - abolition of slave trade in GB\ rotation=90, color='black', va='top')\r\n\r\nplt.axvline(x=1833, color='black', linestyle='--', linewidth=1)\r\nplt.text(1833 + 2, plt.ylim()[1] * 0.99, \"1833 - abolition of slavery in GB\ rotation=90, color='black', va='top')\r\n\r\n# Title and labels\r\nplt.title(\"Number of Embarked Slaves per Country (Annual Data with 15-Year Smoothing)\")\r\nplt.xlabel(\"Year\")\ ...
now display it as a ratio per country for every year, bar chart
display it always like this from the bottom: Great Britain, France, Portugal, Spain, Netherlands, USA
and now without great britian
but also consider the Anteil of GB, for the percentages, but dont show them
and now dont show it as percentages, but as whole numbers
For both files:\r\nPLACE_CONSTRUCTED_UK.gpkg\r\nPLACE_REGISTERED_UK.gpkg\r\ndelete duplicate entries (where shipname and date constructed/dateregistered are the same), keep only the entry with the lowest ID
export to the same folder, but with dropDUPL as the last part of the name
now for both files create a linechart like above, summing up all values per year
and now like above per country, NATINIMP
Do it like this:\r\n# Remove duplicate vessels based on SHIPNAME, PLACREG, and PLACCONS\r\nunique_vessels_REG = places_registered_UK.drop_duplicates(subset=[\"SHIPNAME\ \"PLACREG\ \"YRREG\"])\r\nunique_vessels_CONS = places_constructed_UK.drop_duplicates(subset=[\"SHIPNAME\ \"PLACCONS\ \"YRCONS\"])\r\n\r\n# Filter to include only rows where YRREG >= 1750\r\nfiltered_vessels_REG = unique_vessels_REG[unique_vessels_REG[\"YRREG\"] >= 1750]\r\nfiltered_vessels_CONS = unique_vessels_CONS[unique_vessels_CONS[\"YRCONS\"] >= 1750]\r\n\r\n# Grouping for unsmoothed data\r\nunsmoothed_reg = filtered_vessels_REG.groupby(\"YRREG\")[\"PLACREG\"].count()\r\nunsmoothed_cons = filtered_vessels_CONS.groupby(\"YRCONS\")[\"PLACCONS\"].count()\r\n\r\n# 3-year moving average smoothing (average of previous, current, and next year)\r\nsmoothed_reg = unsmoothed_reg.rolling(window=3, center=True, min_periods=1).mean()\r\nsmoothed_cons = unsmoothed_cons.rolling(window=3, center=True, min_periods=1).mean()\r\n\r\n# Plot for Registered Vessels (PLACREG)\r\nplt.figure(figsize=(12, 6))\r\nplt.plot(unsmoothed_reg.index, unsmoothed_reg, label=\"PLACREG (unsmoothed)\ color=\"blue\ alpha=0.2)\r\nplt.plot(smoothed_reg.index, smoothed_reg, label=\"PLACREG (3-year smoothed)\ color=\"blue\ linestyle=\"--\")\r\nplt.title(\"Number of Registered Vessels (1750 and Later)\")\r\nplt.xlabel(\"Year\")\r\nplt.ylabel(\"Number of Registered Vessels\")\r\nplt.legend()\r\n\r\n# Formatting y-axis with delimiter \\" for thousands\r\nplt.gca().get_yaxis().set_major_formatter(mt.FuncFormatter(lambda x, _: format(int(x), ',')))\r\n\r\n# Set x-axis ticks to show years every 5 years\r\nplt.xticks(range(int(filtered_vessels_REG[\"YRREG\"].min()), int(filtered_vessels_REG[\"YRREG\"].max()) + 1, 5), rotation=45)\r\n\r\nplt.tight_layout()\r\nplt.show()\r\n\r\n# Plot for Constructed Vessels (PLACCONS)\r\nplt.figure(figsize=(12, 6))\r\nplt.plot(unsmoothed_cons.index, unsmoothed_cons, label=\"PLACCONS (unsmoothed)\ co ...
Now do this, but divided by country in NATINIMP\r\nimport matplotlib.pyplot as plt\r\nimport matplotlib.ticker as mt\r\nimport pandas as pd\r\n\r\n# Load deduplicated files\r\nplaces_registered_UK = gpd.read_file(r\"C:\\Users\\andre\\OneDrive\\Dokumente\\UNI\\Master_Digital_Humanities\\S4_SS2025\\SE_Seminar_zur_Abschlussarbeit\\Masterarbeit\\data\\PLACE_REGISTERED_UK_dropDUPL.gpkg\")\r\nplaces_constructed_UK = gpd.read_file(r\"C:\\Users\\andre\\OneDrive\\Dokumente\\UNI\\Master_Digital_Humanities\\S4_SS2025\\SE_Seminar_zur_Abschlussarbeit\\Masterarbeit\\data\\PLACE_CONSTRUCTED_UK_dropDUPL.gpkg\")\r\n\r\n# Remove duplicate vessels based on SHIPNAME, PLACREG, and PLACCONS\r\nunique_vessels_REG = places_registered_UK.drop_duplicates(subset=[\"SHIPNAME\ \"PLACREG\ \"YRREG\"])\r\nunique_vessels_CONS = places_constructed_UK.drop_duplicates(subset=[\"SHIPNAME\ \"PLACCONS\ \"YRCONS\"])\r\n\r\n# Filter to include only rows where YRREG >= 1750\r\nfiltered_vessels_REG = unique_vessels_REG[unique_vessels_REG[\"YRREG\"] >= 1750]\r\nfiltered_vessels_CONS = unique_vessels_CONS[unique_vessels_CONS[\"YRCONS\"] >= 1750]\r\n\r\n# Grouping for unsmoothed data\r\nunsmoothed_reg = filtered_vessels_REG.groupby(\"YRREG\")[\"PLACREG\"].count()\r\nunsmoothed_cons = filtered_vessels_CONS.groupby(\"YRCONS\")[\"PLACCONS\"].count()\r\n\r\n# 3-year moving average smoothing (average of previous, current, and next year)\r\nsmoothed_reg = unsmoothed_reg.rolling(window=3, center=True, min_periods=1).mean()\r\nsmoothed_cons = unsmoothed_cons.rolling(window=3, center=True, min_periods=1).mean()\r\n\r\n# Plot for Registered Vessels (PLACREG)\r\nplt.figure(figsize=(12, 6))\r\nplt.plot(unsmoothed_reg.index, unsmoothed_reg, label=\"PLACREG (unsmoothed)\ color=\"blue\ alpha=0.2)\r\nplt.plot(smoothed_reg.index, smoothed_reg, label=\"PLACREG (3-year smoothed)\ color=\"blue\ linestyle=\"--\")\r\nplt.title(\"Number of Registered Vessels (1750 and Later)\")\r\nplt.xlabel(\"Year\")\r\nplt.ylabel(\"Number  ...
like this:\r\nimport geopandas as gpd\r\nimport os\r\nimport pandas as pd\r\n\r\n# Path to yearly shapefiles for colonies\r\ncolonies_folder = r\"C:\\Users\\andre\\OneDrive\\Dokumente\\UNI\\Master_Digital_Humanities\\S4_SS2025\\SE_Seminar_zur_Abschlussarbeit\\Masterarbeit\\data\\colonies\\yearly_shapefiles\"\r\n\r\n# Prepare list to collect filtered journeys\r\nfiltered_journeys = []\r\n\r\nfor year in range(1700, 1870):\r\n shp_path = os.path.join(colonies_folder, f\"colonies_{year}.gpkg\")\r\n if not os.path.exists(shp_path):\r\n  continue\r\n\r\n # Load the yearly colonies shapefile\r\n colonies_gdf = gpd.read_file(shp_path)\r\n\r\n # Filter for British colonies (case-insensitive, works for 'British' or 'United Kingdom')\r\n british = colonies_gdf[\r\n  colonies_gdf['colonizer_grouped'].str.lower().isin(['british', 'united kingdom'])\r\n ]\r\n british = british[british.geometry.notnull()]\r\n if british.empty:\r\n  continue\r\n\r\n # Filter journeys for this year\r\n journeys_this_year = gdf[gdf['YEARAM'] == year].copy()\r\n if journeys_this_year.empty:\r\n  continue\r\n\r\n # Spatial join: keep only journeys whose destination is within a British colony polygon\r\n joined = gpd.sjoin(\r\n  journeys_this_year.set_geometry('geometry'),\r\n  british[['geometry']],\r\n  how='inner',\r\n  predicate='within'\r\n )\r\n if not joined.empty:\r\n  filtered_journeys.append(joined)\r\n\r\n# Concatenate all filtered journeys into a single GeoDataFrame\r\nif filtered_journeys:\r\n british_journeys_gdf = gpd.GeoDataFrame(pd.concat(filtered_journeys, ignore_index=True), crs=gdf.crs)\r\nelse:\r\n british_journeys_gdf = gpd.GeoDataFrame(columns=gdf.columns, crs=gdf.crs)\r\n\r\n# Export all journeys to British colonies as a single GeoPackage file\r\noutput_path = r\"C:\\Users\\andre\\OneDrive\\Dokumente\\UNI\\Master_Digital_Humanities\\S4_SS2025\\SE_Seminar_zur_Abschlussarbeit\\Mast ...
could you do it the other way around: not going through the dataset by index, but by year, this is faster, because each shapefile has only to be load once?
align values in stdb3_colonial_power_XXX columns with the following:\r\ncolor_mapping = {\r\n 'Denmark / Baltic': 'wheat',\r\n 'France': 'navy',\r\n \"Great Britain\": \"firebrick\\r\n \"Netherlands\": \"darkorange\\r\n 'Portugal / Brazil': 'olivedrab',\r\n 'Spain / Uruguay': 'limegreen',\r\n 'USA': 'darkorchid',\r\n \"Other\": \"grey\"\r\n}
no, just use those exact country names and replace the other ones:\r\nUnited Kingdom7962\r\nPortugal3474\r\nSpain2747\r\nzzzz. No longer colonized 2003\r\nNetherlands1134\r\nFrance  1085\r\nz. Multiple colonizers  127\r\nzzz. Not colonized 25\r\nzz. Colonizer 8
now give me a df, where buy = sel and one where they are not the same
only plot 1750 - 1870
only for great britain
colours from the color map above please
I mean this one:\r\nclist_css = [\"#f5deb3\\"#000080\\"#b22222\\"#ff8c00\\"#808080\\"#6b8e23\\"#9932cc\\"#32cd32\"]\r\n#as colormap\r\ncmap= ListedColormap(clist)\r\n#as diotionary\r\ncolor_mapping = {\r\n 'Denmark / Baltic': 'wheat',\r\n 'France': 'navy',\r\n \"Great Britain\": \"firebrick\\r\n \"Netherlands\": \"darkorange\\r\n 'Portugal / Brazil': 'olivedrab',\r\n 'Spain / Uruguay': 'limegreen',\r\n 'USA': 'darkorchid',\r\n \"Other\": \"grey\"\r\n}
and now please the ratio of all countries to GB colonies, start with stdb3
as bar charts please
welche Datenquelle nutze ich hier am Ende, um die Export-Files zu erzeugen?
gdf_all_landing_ports_4326 calculate lon lat columns from point geometry
filter this to landing ports only in the americas gdf_all_landing_ports_4326
gdf_americas_landing_ports_4326.plot(kind='scatter', x='lon', y='lat', alpha=0.5, figsize=(10, 6)) but use americas_bbox_4326 for display
here change colour scheme of points, use mean of year for legend description (cluster 0 (mean: XX))
could you change the cluster number, so the means are in rising order?
plot each cluster in one of 4 frames in an image
why is africa so white, and america looks normal?
can i use a robinson projection
in the legend dont use the transperancy
in this block, use the colourmap from the beginning of the file
yes, but the Reihenfolge should also bo like that in the plot
which of the columns are really used in one of the files?
For gdf_all_landing_ports keep only all columns needed in this file
stdb3_ports.reset_index() and rename to VOYAGEID
take data from this folder clustering_kmeans4, geopackages files and contract lines based on same MJSELIMP and CLUSTER_ID, add a count column.
do this per shapefile
starting with sv_grouped_filtered_START calculate the share of all powers to british colonies
for the whole period
This, but split up for the values of london and liverpool\r\n#plotting in one plot both registered and constructed vessels\r\n\r\nplt.figure(figsize=(12, 6))\r\n\r\n#plot unsmoothed data\r\nplt.plot(unsmoothed_reg.index, unsmoothed_reg, label=\"registered\ color=\"blue\ alpha=0.2)\r\nplt.plot(unsmoothed_cons.index, unsmoothed_cons, label=\"constructed\ color=\"red\ alpha=0.2)\r\n\r\n#plot smoothed data\r\nplt.plot(smoothed_reg.index, smoothed_reg, label=\"registered (3-year smoothed)\ color=\"blue\ linestyle=\"--\")\r\nplt.plot(smoothed_cons.index, smoothed_cons, label=\"constructed (3-year smoothed)\ color=\"red\ linestyle=\"--\")\r\n\r\nplt.title(\"Number of Vessels by Registration and Construction Place (1750 and Later in GB)\ size=20)\r\nplt.xlabel(\"Year\")\r\nplt.ylabel(\"Number of Vessels\")\r\nplt.legend()\r\n\r\nplt.gca().get_yaxis().set_major_formatter(mticker.FuncFormatter(lambda x, _: format(int(x), ',')))\r\n\r\nplt.xticks(range(int(filtered_vessels_REG[\"YRREG\"].min()), int(filtered_vessels_REG[\"YRREG\"].max()) + 1, 5), rotation=45)\r\n\r\n# Show plot\r\nplt.tight_layout()\r\nplt.show()
To this, add a 5 year smoothing:\r\n#plotting number of voyages per year where buy colonial power == sel colonial power\r\n\r\n#count all voyages per year (total trade)\r\ntotal_per_year = stdb3.groupby(\"year\")[\"country\"].count()\r\n\r\n# Count: voyages where buy == sel == country (your filtered subset)\r\nsame_nat_per_year = stdb3_same_NATINIMP.groupby(\"year\")[\"country\"].count()\r\n\r\n# Calculate ratio (share)\r\nshare_per_year = same_nat_per_year / total_per_year\r\n\r\n# Filter for years 1750-1870\r\nyears_mask = (share_per_year.index >= 1600) & (share_per_year.index <= 1870)\r\nshare_per_year_filtered = share_per_year.loc[years_mask]\r\n\r\nplt.figure(figsize=(12, 6))\r\nplt.plot(share_per_year_filtered.index, share_per_year_filtered, label=\"share of total trade\ color=\"green\")\r\nplt.ylabel(\"ratio\")\r\nplt.xlabel(\"year\")\r\nplt.title(\"ratio of buy == sel colonial power (1750-1870)\")\r\nplt.legend()\r\nplt.show()
calculate the percentage of british journeys to british colonies between 1791 and 1810
and give me the year with the lowest number
percentage wise
and before 1807
How do i create this plot from my data?
start with gdf_all_landing_ports
like i have, but use colour from color_mapping
but the reihenfolge is different, e.g. portugal is not the first colour there